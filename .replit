modules = ["python-3.12", "nodejs-20", "web"]
run = "uvicorn undefined:app --host 0.0.0.0 --port 3000"

[nix]
channel = "stable-24_05"
packages = ["chromedriver", "chromium", "geckodriver", "rapidfuzz-cpp", "taskflow"]

[deployment]
run = ["sh", "-c", "uvicorn undefined:app --host 0.0.0.0 --port 3000"]

[workflows]
runButton = "Run"

[[workflows.workflow]]
name = "Run"
author = 43402999
mode = "sequential"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "python -m uvicorn api:app --host 0.0.0.0 --port 5000"
